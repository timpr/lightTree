#include "stdint.h"
#include "Arduino.h"

//TODO: REENABLE
// const static uint8_t ON = LOW;
// const static uint8_t OFF = HIGH;

const static uint8_t ON = HIGH;
const static uint8_t OFF = LOW;

int const static startIndex = 1;
int const static endIndex = 8;
int const static treeStartIndex = startIndex;
int const static treeEndIndex = 4;
int const static decorStartIndex = treeEndIndex + 1;
int const static decorEndIndex = endIndex;
